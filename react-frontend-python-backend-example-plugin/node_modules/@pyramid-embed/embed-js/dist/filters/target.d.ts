import { Filter } from './filter';
import { GlobalEmbedReportTarget } from '../types/types';
export declare class Target {
    private targets;
    private constructor();
    static create(): Target;
    static byFilter(filters: Filter, name: string, isOlap?: boolean): Target;
    add(filters: Filter, name: string, isOlap?: boolean): Target;
    toGlobalEmbedTarget(): GlobalEmbedReportTarget[];
}
