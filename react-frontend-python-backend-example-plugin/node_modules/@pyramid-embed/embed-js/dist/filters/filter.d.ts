import { GlobalEmbedReportFilter } from '../types/types';
export declare class Filter {
    private filters;
    private constructor();
    static create(): Filter;
    static byUniqueName(uniqueName: string): Filter;
    static by(dimension: string, hierarchy: string, member: string): Filter;
    addUniqueName(uniqueName: string): Filter;
    add(dimension: string, hierarchy: string, member: string): Filter;
    toGlobalEmbedFilters(): GlobalEmbedReportFilter[];
}
