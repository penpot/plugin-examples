import { EmbedHubAPI } from './types/types';
export declare class HubClient {
    private hubAPI;
    constructor(api: EmbedHubAPI);
    changeSelectedTab(tabIdx: number): void;
    getSelectedTab(): number;
    getTabsList(): {
        tabName: string;
    }[];
}
